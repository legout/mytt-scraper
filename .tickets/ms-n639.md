---
id: ms-n639
status: in_progress
deps: [ms-zp93]
links: []
created: 2026-03-09T01:22:57Z
type: feature
priority: 0
assignee: legout
parent: ms-mvjw
tags: [feature, vertical-slice, web, auth]
---
# Deliver login/logout flow and protected route enforcement

Build authenticated access with login form, SSE auth status updates, logout handling, and auth guard on protected routes.

## Design

PRD US-1 + 4.3; Spec 2.1(auth),2.2(session),3.1; Plan P1.1-P1.5

## Acceptance Criteria

- [ ] /login renders validated credentials form with loading feedback
- [ ] Login POST runs blocking auth via asyncio.to_thread()
- [ ] Successful auth persists session user and redirects
- [ ] Invalid auth shows user-visible error state via SSE
- [ ] /logout clears session and protected routes redirect if unauthenticated


## Notes

**2026-03-09T07:29:40Z**

Implementation complete with all critical/major issues fixed:
- Login page with SSE streaming for auth status updates
- Logout handler with session clearing
- Session signals for auth state management
- Protected routes with inline auth checks (StarHTML decorator workaround)
- Playwright cookies captured and stored in session
- 24/24 core tests pass, 3 LoginHandler SSE tests skipped

Key files: auth.py, search.py, tables.py, signals/session.py, test_auth_routes.py
Commit: faf2bcf

BLOCKER: Gate=Uncertain - SSE login path not fully verified via automated tests (TestClient SSE limitations). Needs manual runtime verification of login flow before closing.
